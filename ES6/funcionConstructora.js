    //Solo cambie el código debajo de esta línea

    class Vegetable {
    constructor(name) {
    this.name = name;
    }
    }

    //Solo cambia el código arriba de esta línea
    const carrot = new Vegetable("carrot");
    console.log(carrot.name); // Debería ser 'carrot'



//Use la sintaxis de clase para definir una función constructora

//Use la class palabra clave y escriba a constructor para crear la Vegetable clase. La Vegetable clase le permite crear un objeto vegetal con una propiedad name que se pasa al constructor.

//Original:
// Only change code below this line
// Only change code above this line
//const carrot = new Vegetable('carrot');
//console.log(carrot.name); // Debería ser 'carrot'